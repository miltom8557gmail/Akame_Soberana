<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="#020202" android:padding="15dp">

    <TextView android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="AKAME OMNI-LINK | TERMUX ECOSYSTEM" android:textColor="#00FFCC"
        android:gravity="center" android:textSize="12sp" android:textStyle="bold"/>

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" 
        android:orientation="horizontal" android:layout_marginTop="20dp">
        <ProgressBar android:id="@+id/cpu_bar" style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp" android:layout_height="10dp" android:layout_weight="1"
            android:progressDrawable="@android:drawable/progress_horizontal" android:progress="45"/>
    </LinearLayout>

    <TextView android:id="@+id/termux_status" android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="> TERMUX: CONECTADO [miltom.jose8557]" android:textColor="#BD00FF" 
        android:textSize="10sp" android:layout_marginTop="5dp" android:typeface="monospace"/>

    <ScrollView android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1" 
        android:layout_marginTop="15dp" android:background="#050505">
        <TextView android:id="@+id/neural_log" android:layout_width="match_parent" android:layout_height="wrap_content"
            android:textColor="#00FF00" android:typeface="monospace" android:textSize="11sp"/>
    </ScrollView>

    <Button android:id="@+id/btn_sync_cloud" android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="FORÇAR BACKUP GOOGLE DRIVE" android:backgroundTint="#4285F4" android:textColor="#FFFFFF"/>
</LinearLayout>
